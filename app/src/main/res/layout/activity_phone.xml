<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@color/white">

    <!-- Título de la actividad -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/activity_phone"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="@color/colorPrimary"
        android:gravity="center"
        android:layout_marginBottom="16dp" />

    <!-- Estado del permiso de teléfono -->
    <TextView
        android:id="@+id/tvPhoneStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Estado del permiso de teléfono"
        android:textSize="16sp"
        android:gravity="center"
        android:layout_marginBottom="24dp" />

    <!-- Contenedor para los elementos relacionados con la llamada -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center">

        <!-- Campo para el número de teléfono -->
        <EditText
            android:id="@+id/etPhoneNumber"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Número de teléfono (ej: 123456789)"
            android:inputType="phone"
            android:layout_marginBottom="24dp"
            android:background="@color/white" />

        <!-- Botón para realizar la llamada -->
        <Button
            android:id="@+id/btnMakeCall"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:text="@string/btn_make_call"
            android:layout_marginBottom="16dp"
            android:background="@color/white" />

        <!-- Botón para solicitar permisos -->
        <Button
            android:id="@+id/btnRequestPermission"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:text="@string/btn_grant_permission"
            android:visibility="gone"
            android:background="@color/white" />

    </LinearLayout>

    <!-- Mensaje de seguridad -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="🔒 Solo se simula la llamada - No se realizan llamadas reales"
        android:textSize="12sp"
        android:gravity="center"
        android:textColor="@android:color/darker_gray" />

</LinearLayout>
